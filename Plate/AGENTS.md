# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Testing

No need to screen record when testing. Just test the change normally by running it and observing the result (screenshots are fine when useful).
